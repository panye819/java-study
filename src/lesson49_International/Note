1、国际化：
	国际化程序：
		一套程序可以适应多个不同国家人的语言要求，那么这套程序称为国际化程序。
		如果要想实现国际化程序，则必须依靠Locale、ResourceBoundle、MessageFormat几个完成，并结合属性
		文件（*.properties）。
2、Locale：
	locale表示的是一个地区，也就是说在国际化程序中通过locale指定当前所在的地区。
	每个国家都存在一个国家编号。
	